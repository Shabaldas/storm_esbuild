<%= turbo_frame_tag :results, data: { turbo_action: 'advance' } do %>
  <div id="products" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:ml-10 pb-16"></div>
  <%= turbo_frame_tag :pagination, loading: :lazy,
                      src: products_path(format: :turbo_stream, q: params[:q]&.to_unsafe_h) %>
<% end %>